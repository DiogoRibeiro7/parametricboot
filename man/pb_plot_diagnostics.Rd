% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pb_plot_diagnostics.R
\name{pb_plot_diagnostics}
\alias{pb_plot_diagnostics}
\title{Plot bootstrap diagnostics}
\usage{
pb_plot_diagnostics(results, parameter = NULL)
}
\arguments{
\item{results}{A \code{pb_boot} object from \code{\link[=pb_simulate]{pb_simulate()}} or \code{\link[=pb_parallel]{pb_parallel()}}.}

\item{parameter}{Character vector of coefficient names to plot. The default,
\code{NULL}, plots all of them.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws, for each coefficient, the running mean of the bootstrap estimates
against the number of replicates. The running mean settles down once enough
replicates have been drawn, so a curve that is still drifting at the right
edge suggests increasing \code{n}. The grey points are the individual estimates
and the dashed line is the original estimate; a persistent gap between the
curve and the dashed line is the bootstrap estimate of bias.
}
\examples{
fit <- glm(vs ~ mpg, data = mtcars, family = binomial())
res <- pb_simulate(fit, n = 100, seed = 1)
pb_plot_diagnostics(res)
}
