% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pb_simulate.R
\name{pb_simulate}
\alias{pb_simulate}
\title{Run parametric bootstrap replicates}
\usage{
pb_simulate(model, n = 100, ..., seed = NULL, keep_fits = TRUE)
}
\arguments{
\item{model}{A fitted model from \code{\link[stats:lm]{stats::lm()}}, \code{\link[stats:glm]{stats::glm()}},
\code{\link[lme4:lmer]{lme4::lmer()}}, \code{\link[lme4:glmer]{lme4::glmer()}} or \code{\link[survival:coxph]{survival::coxph()}}.}

\item{n}{Number of bootstrap replicates.}

\item{...}{Additional arguments used when refitting: they override
arguments of the original call for \code{lm}, \code{glm} and \code{coxph} models, and are
passed to \code{\link[lme4:refit]{lme4::refit()}} for mixed models.}

\item{seed}{Optional single number. If supplied, the replicates are
reproducible and the state of the global random number generator is left
untouched.}

\item{keep_fits}{Should the refitted models be stored? They are needed by
\code{\link[=pb_predict_boot]{pb_predict_boot()}}, \code{\link[=pb_plot_predictions]{pb_plot_predictions()}} and \code{\link[=pb_compare_models]{pb_compare_models()}},
but can use a lot of memory for large \code{n}.}
}
\value{
An object of class \code{pb_boot}: a list with elements
\describe{
\item{\code{original}}{the fitted model.}
\item{\code{replicates}}{list of \code{n} refitted models (\code{NULL} for failed
refits), or \code{NULL} if \code{keep_fits = FALSE}.}
\item{\code{estimates}, \code{std_errors}}{\code{n} by \code{p} matrices of parameter
estimates and their standard errors. For mixed models the parameters
are the fixed effects followed by the variance components, whose
standard errors are \code{NA}.}
\item{\code{failed}}{logical vector flagging the replicates that are excluded
from all summaries: refits that threw an error, and replicates removed
by \code{\link[=pb_drop_warned]{pb_drop_warned()}}.}
\item{\code{warned}}{logical vector flagging the refits that produced
warnings.}
\item{\code{n}, \code{seed}, \code{call}}{the number of replicates, the seed and the
call.}
}
}
\description{
Simulates \code{n} new responses from a fitted model, refits the model to each
of them and collects the resulting coefficient estimates and standard
errors.
}
\details{
Each replicate draws a response from the fitted model and refits the model
to it, holding the covariates fixed.
\itemize{
\item For \code{lm} and \code{glm} models the response is drawn with \code{\link[stats:simulate]{stats::simulate()}}
and the original call is re-evaluated on the original data, so
transformed terms (\code{log(x)}, \code{poly(x, 2)}), \code{offset()}, \code{weights},
\code{subset} and two-column binomial responses are all supported. The model
must have been fitted with a \code{data} argument.
\item For \code{lmer} and \code{glmer} models the response is drawn with the 'lme4'
\code{simulate()} method (new random effects are drawn for every replicate)
and the model is refitted with \code{\link[lme4:refit]{lme4::refit()}}, exactly as
\code{\link[lme4:bootMer]{lme4::bootMer()}} does. Besides the fixed effects, the variance
components are tracked: the random-effect standard deviations and
correlations and, if the family has one, the residual standard deviation.
They are named as in \code{confint(model, oldNames = FALSE)}, for example
\code{sd_(Intercept)|Subject}, \code{cor_Days.(Intercept)|Subject} and \code{sigma}.
'lme4' provides no standard errors for them, so summaries that need one
(the Wald \code{coverage} and studentised intervals) are \code{NA} for these rows.
\item For \code{coxph} models there is no fully parametric model to simulate from, so
the model-based resampling scheme of Davison and Hinkley (1997,
Algorithm 7.3) is used: failure times are drawn from the fitted survivor
function based on the Breslow baseline hazard, and censoring times from
the Kaplan-Meier estimate of the censoring distribution, conditional on
the observed censoring pattern. In a model with \code{strata()}, each stratum
has its own baseline hazard, censoring distribution and end of follow-up.
Only right-censored, unweighted models are supported, without \code{tt()},
penalised or \code{frailty()} terms, strata-by-covariate interactions or
\code{cluster()} (the observations are resampled independently).
}

A refit that throws an error is recorded as failed and dropped from all
summaries; a single warning reports how many refits failed or produced
warnings (for example convergence problems). Replicates with warnings are
kept; see \code{\link[=pb_drop_warned]{pb_drop_warned()}} for when and how to remove them.
}
\examples{
fit <- glm(vs ~ mpg, data = mtcars, family = binomial())
res <- pb_simulate(fit, n = 50, seed = 1)
res

pb_summary_table(res)
}
\references{
Davison, A. C. and Hinkley, D. V. (1997) \emph{Bootstrap Methods and their
Application}. Cambridge University Press.
}
\seealso{
\code{\link[=pb_parallel]{pb_parallel()}} to run the refits on several cores;
\code{\link[=pb_summary_table]{pb_summary_table()}}, \code{\link[=pb_confint]{pb_confint()}} and \code{\link[=pb_plot_estimates]{pb_plot_estimates()}} to
summarise the result.
}
