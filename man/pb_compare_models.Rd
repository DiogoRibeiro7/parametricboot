% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pb_compare_models.R
\name{pb_compare_models}
\alias{pb_compare_models}
\title{Compare models by the bootstrap distribution of a fit metric}
\usage{
pb_compare_models(
  models,
  n = 100,
  metric = stats::AIC,
  level = 0.95,
  seed = NULL,
  ...
)
}
\arguments{
\item{models}{A non-empty list of fitted models supported by
\code{\link[=pb_simulate]{pb_simulate()}}. Names, if present, are used to label the models.}

\item{n}{Number of bootstrap replicates for each model.}

\item{metric}{A function that takes a fitted model and returns a single
number, such as \code{\link[stats:AIC]{stats::AIC()}}, \code{\link[stats:AIC]{stats::BIC()}} or \code{\link[stats:deviance]{stats::deviance()}}.}

\item{level}{Level of the percentile interval reported for the metric.}

\item{seed}{Optional single number making the comparison reproducible.}

\item{...}{Additional arguments passed to \code{\link[=pb_simulate]{pb_simulate()}}.}
}
\value{
A data frame with one row per model and columns \code{model},
\code{observed} (the metric of the original fit), \code{boot_mean}, \code{boot_sd},
\code{lower} and \code{upper} (summaries of the metric over the bootstrap refits).
}
\description{
Runs a parametric bootstrap for each model and evaluates \code{metric} on the
original fit and on every refit. This shows how much a criterion such as
the AIC varies from sample to sample under each fitted model, which helps
to judge whether an observed difference between models is meaningful.
}
\examples{
fits <- list(
  mpg = glm(vs ~ mpg, data = mtcars, family = binomial()),
  mpg_wt = glm(vs ~ mpg + wt, data = mtcars, family = binomial())
)
pb_compare_models(fits, n = 25, seed = 1)
}
\seealso{
\code{\link[=pb_lrt]{pb_lrt()}} for a formal test when the models are nested.
}
