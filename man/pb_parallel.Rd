% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pb_parallel.R
\name{pb_parallel}
\alias{pb_parallel}
\title{Run parametric bootstrap replicates in parallel}
\usage{
pb_parallel(model, n = 100, workers = 2, ..., seed = NULL, keep_fits = TRUE)
}
\arguments{
\item{model}{A fitted model from \code{\link[stats:lm]{stats::lm()}}, \code{\link[stats:glm]{stats::glm()}},
\code{\link[lme4:lmer]{lme4::lmer()}}, \code{\link[lme4:glmer]{lme4::glmer()}} or \code{\link[survival:coxph]{survival::coxph()}}.}

\item{n}{Number of bootstrap replicates.}

\item{workers}{Number of parallel worker processes.}

\item{...}{Additional arguments used when refitting: they override
arguments of the original call for \code{lm}, \code{glm} and \code{coxph} models, and are
passed to \code{\link[lme4:refit]{lme4::refit()}} for mixed models.}

\item{seed}{Optional single number. If supplied, the replicates are
reproducible and the state of the global random number generator is left
untouched.}

\item{keep_fits}{Should the refitted models be stored? They are needed by
\code{\link[=pb_predict_boot]{pb_predict_boot()}}, \code{\link[=pb_plot_predictions]{pb_plot_predictions()}} and \code{\link[=pb_compare_models]{pb_compare_models()}},
but can use a lot of memory for large \code{n}.}
}
\value{
An object of class \code{pb_boot}; see \code{\link[=pb_simulate]{pb_simulate()}}.
}
\description{
A drop-in replacement for \code{\link[=pb_simulate]{pb_simulate()}} that distributes the model refits
over a local cluster of R processes.
}
\details{
The bootstrap responses are simulated in the calling process and only the
refits are distributed, so for a given \code{seed} the result is identical to
that of \code{\link[=pb_simulate]{pb_simulate()}}, whatever the number of workers.

The workers are fresh R sessions: 'parametricboot' must be installed (not
just loaded with \code{devtools::load_all()}), and everything the model call
needs, such as weights or offsets, should be a column of \code{data} rather than
a variable in the global environment.

Parallel execution has a start-up cost, so it only pays off when a single
refit is slow, as is typical for mixed models.
}
\examples{
\donttest{
fit <- glm(vs ~ mpg, data = mtcars, family = binomial())
res <- pb_parallel(fit, n = 50, workers = 2, seed = 1)
res
}
}
