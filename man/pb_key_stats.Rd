% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pb_key_stats.R
\name{pb_key_stats}
\alias{pb_key_stats}
\title{Bootstrap bias, standard error, MSE and coverage}
\usage{
pb_key_stats(results, level = 0.95)
}
\arguments{
\item{results}{A \code{pb_boot} object from \code{\link[=pb_simulate]{pb_simulate()}} or \code{\link[=pb_parallel]{pb_parallel()}}.}

\item{level}{Nominal level of the Wald intervals whose coverage is
estimated.}
}
\value{
A data frame with one row per coefficient and columns
\describe{
\item{\code{term}}{coefficient name.}
\item{\code{estimate}}{original estimate \eqn{\hat\theta}.}
\item{\code{boot_mean}}{mean of the bootstrap estimates.}
\item{\code{bias}}{\code{boot_mean - estimate}.}
\item{\code{std_error}}{standard deviation of the bootstrap estimates.}
\item{\code{mse}}{mean squared error of the bootstrap estimates around
\code{estimate}.}
\item{\code{coverage}}{proportion of replicates whose Wald interval
(estimate plus or minus a normal quantile times the standard error)
contains \code{estimate}. Values far from \code{level} indicate that the usual
Wald intervals are unreliable for this model.}
}
}
\description{
In a parametric bootstrap the fitted model plays the role of the truth, so
the behaviour of the estimator around the original estimate
\eqn{\hat\theta} estimates its behaviour around the true parameter.
}
\examples{
fit <- glm(vs ~ mpg, data = mtcars, family = binomial())
res <- pb_simulate(fit, n = 50, seed = 1)
pb_key_stats(res)
}
