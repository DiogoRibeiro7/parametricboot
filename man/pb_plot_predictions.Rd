% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pb_plot_predictions.R
\name{pb_plot_predictions}
\alias{pb_plot_predictions}
\title{Plot predictions with bootstrap intervals}
\usage{
pb_plot_predictions(results, newdata, x = NULL, level = 0.95, ...)
}
\arguments{
\item{results}{A \code{pb_boot} object from \code{\link[=pb_simulate]{pb_simulate()}} or \code{\link[=pb_parallel]{pb_parallel()}}.}

\item{newdata}{Data frame to predict for.}

\item{x}{Name of the column of \code{newdata} to put on the horizontal axis. A
numeric column gives a line with a ribbon; any other column, or the
default \code{NULL} (row number), gives points with error bars.}

\item{level}{Level of the bootstrap percentile interval.}

\item{...}{Additional arguments passed to \code{\link[stats:predict]{stats::predict()}}, such as
\code{type = "response"}, or \code{re.form = NA} for population-level predictions
from mixed models.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the predictions of the original model for \code{newdata} together with
percentile intervals computed from the bootstrap refits.
}
\examples{
fit <- glm(vs ~ mpg, data = mtcars, family = binomial())
res <- pb_simulate(fit, n = 100, seed = 1)

new_cars <- data.frame(mpg = seq(10, 35, by = 1))
pb_plot_predictions(res, new_cars, x = "mpg", type = "response")
}
