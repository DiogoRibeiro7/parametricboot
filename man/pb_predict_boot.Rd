% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pb_predict_boot.R
\name{pb_predict_boot}
\alias{pb_predict_boot}
\title{Predict with bootstrap uncertainty}
\usage{
pb_predict_boot(results, newdata = NULL, ...)
}
\arguments{
\item{results}{A \code{pb_boot} object from \code{\link[=pb_simulate]{pb_simulate()}} or \code{\link[=pb_parallel]{pb_parallel()}}.}

\item{newdata}{Optional data frame to predict for. If \code{NULL}, predictions
are made for the observations used to fit the model.}

\item{...}{Additional arguments passed to \code{\link[stats:predict]{stats::predict()}}, such as
\code{type = "response"}, or \code{re.form = NA} for population-level predictions
from mixed models.}
}
\value{
A numeric matrix with one row per observation and one column per
successful bootstrap replicate.
}
\description{
Computes predictions from every refitted bootstrap model.
}
\examples{
fit <- glm(vs ~ mpg, data = mtcars, family = binomial())
res <- pb_simulate(fit, n = 50, seed = 1)

new_cars <- data.frame(mpg = c(15, 20, 25))
preds <- pb_predict_boot(res, new_cars, type = "response")
dim(preds)

# 95\% percentile intervals for the predicted probabilities
t(apply(preds, 1, quantile, probs = c(0.025, 0.975)))
}
